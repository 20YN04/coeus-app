[pytest]
# Zet de repo-root op sys.path zodat `import main` / `from brain...` werkt, ongeacht
# of pytest als `pytest` of `python -m pytest` draait (CI gebruikt het eerste).
pythonpath = .
testpaths = tests
